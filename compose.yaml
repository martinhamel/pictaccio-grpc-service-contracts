services:
  pictaccio-grpc-service-contracts:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app:z
    ports:
      - "8000:8000"
    environment:
      - ENV=development